# **C-EASY-1各部分问题的回答**
---
## Part-1
1. 
- GCC本质是一个编译器（原生于LINUX系统），可以把C/C++的源代码翻译成机器能看懂的二进制程序       (eg:.exe文件)
- MinGW本质是一个把 GCC 移植到 Windows 平台的工具包。MinGW 打包了 Windows 版的 GCC 编译器 + Windows 必要的底层库。让程序员在Windows系统里可以使用gcc进行编译，生成.exe文件。 
2. 
- c_cpp_properties.json文件作用：【1】指定编译器路径（MinGW 里的 gcc.exe）
【2】设置 C 语言标准（C99/C11）【3】头文件搜索路径（#include去哪里找头文件）
【4】控制 VS Code 代码高亮、变量提示、红色波浪线报错
- tasks.json作用：定义一个任务 (task)，执行编译命令（等价于手动在终端输入 gcc xxx.c -o xxx.exe）。
他告诉 VS Code：用哪个编译器、编译参数、输出 exe 文件名
- launch.json作用：调试器的配置文件，启动 gdb 调试器，运行已经编译好的 exe。
3. 
- VScode本身只是一个文本编辑器，在电脑本地没有配置插件和编译器的情况下是无法进行程序运行的。虽然理论上说在安装完MinGW后已经可以对代码进行编译，但是仍然缺少调试，提示，报错等功能。因此，我们需要安装C/C++的插件使得VScode可以进行语法错误识别；调试支持（如设置断点）；一键生成上面三个.json文件等功能，让VScode更接近一个IDE。
- 
```jsonc
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gcc.exe - 生成和调试活动文件", //该调试任务的名字
            "type": "cppdbg",//调试器类型
            "request": "launch",//启动程序调试
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",//调试的.exe文件路径
            "args": [],//运行程序传给 main 函数的命令行参数
            "stopAtEntry": false,//程序运行到断点才停下（若改为True则是在main函数第一行停下）
            "cwd": "${workspaceFolder}",//程序运行时的工作目录
            "environment": [],//给要调试运行的 C 语言程序，单独设置环境变量。空集即为不额外设置，直接继承VScode当前的环境
            "externalConsole": false, //外置控制台开关
            "MIMode": "gdb",//指定使用 gdb 作为调试后端
            "miDebuggerPath": "C:\\mingw64\\bin\\gdb.exe",//gdb.exe 的完整路径
            "setupCommands": [
                {
                    "description": "为 gdb 启用整齐打印",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],//gdb 启动时自动执行的调试命令
            "preLaunchTask": "C/C++: gcc.exe build"//启动调试之前要预先执行的任务。
        }
    ]
}
```
*截图*：![alt text](image.png)
        ![alt text](image-1.png)


## Part-2
### 第一部分
1. 
- 变量类型是指规定变量在内存中存储的数据形式、占用内存大小、可以支持的运算。（如int 占4字节，char 占1字节）。变量类型之所以重要是因为它可以**分配对应大小的内存；限定合法运算；防止数据错误，保证程序安全**等等作用。
- 存放年龄用int即可，因为年龄是整数。
- char类型不能存apple因为char只能存单个字符（比如'a'）。apple是一串字符，要用char str[]=apple才可以。
2. 
- 数组元素下标通常从0开始。导致的问题：C语言编译器不会自动报错（没有边界检查），会读取到内存中其他未知数据；写入会篡改其他变量内存。
- 为什么常见且危险：数组越界编译器不会强制检查，属于隐性 bug，有时程序好像正常运行；但是会篡改内存，造成随机崩溃、数据损坏；严重时会被利用造成安全漏洞。
3. 
- 循环：环结构（如 for, while）是如何控制代码块重复执行的：循环是满足条件时重复执行一段代码块，条件不成立就退出循环，从而控制代码可以重复运行。
- for循环基本结构：
``` c
 for(初始化; 条件判断; 迭代){
    循环体;}
```
初始化：循环开始前要定义循环变量（int i=1）
条件判断：每次循环前检查；表达式为真，执行循环体；假则退出
迭代：循环体执行完成后执行，更新循环变量（eg:i++）
- while循环基本结构：
``` c
  while(条件判断){
    循环体;
}
```
- do-while循环基本结构
```c
do{
    循环体;
}while(条件判断);
```
- while 循环和 do…while 循环在首次执行条件判断上有何关键区别:while先判断后执行；do…while至少执行一次。
for 版本：
``` c
#include <stdio.h>
int main
{
int sum = 0;
for(int i = 1; i <= 10; i++){
    sum += i;
    return 0;
}
}
```
while 版本：
```c
#include <stdio.h>
int main
{
int sum = 0;
int i = 1;
while(i <= 10){
    sum += i;
    i++;
}
return 0;
)
```

*总之,for 适合已知循环次数；while 适合循环次数不确定，只知道终止条件。*

4. 

- 算术表达式（a + b * c）
运算对象：数值（整数、浮点数）
运算结果：数值（数字）
作用：做数学计算
逻辑表达式（age>18 && score>=60）
运算对象：关系表达式的真假值（成立 / 不成立）
运算结果：布尔值（真 / 假），（C 语言中非 0 代表真，0 代表假）
作用：条件判断，用于 if、while
- 
|符号|功能|
|:---|:---|
|"&&" 逻辑与|两边都为真，结果才为真；只要一个假，整体假。|
|""逻辑或|任意一边为真，结果就为真；两边全假才是假。|
|"!"逻辑非|取反，真变假，假变真。|
- eg：
``` c
#include <stdio.h>
int main{
int age = 20;
int score = 70;
int res = (age > 18 && score >= 60);
// age>18 真，score>=60 真， res = 1（真）

int age2 = 16;
int res2 = (age2 > 18 || score >= 60);
// age2>18假，score>=60真，整体为真

int res3 = !(age > 18);
// age>18为真，!取反，结果假
}
```

### 第二部分
```c
#include <stdio.h>
int main(void)
{
   int answer;
   do
{
   answer=1;
   int round=0;
   int age;
   char name[100]=""; 
   printf("请输入姓名：\n");
   fgets(name,sizeof(name),stdin);
   for(int i=0;name[i]!='\0';i++)
   {
      if(name[i]=='\n'){name[i]=0;break;}
   }
   printf("请输入年龄：\n");
   scanf("%d",&age);
   while(getchar() != '\n');
   printf("年龄是%d岁,姓名是%s\n",age,name);
   printf("是否要继续输入？(若是则输入1,否则输入0)\n");
   scanf("%d",&answer);
   while(getchar() != '\n');
   round++;
   


}while(answer);

return 0;
}
```
## Part-3
```c
#include <stdio.h>
int average(int a,int b,int c)
{
    int p=0;
    p=(a+b+c)/3;
    return p; 
}

int fangcha(int a,int b,int c)
{
   
    int p=average(a,b,c);
    int f=((p-a)*(p-a)+(p-b)*(p-b)+(p-c)*(p-c))/3;
    return f;
}

int score(int p,int f)
{
    int zh=3 * p - f / 3;
    return zh;
}

void paiming(int x,int y,int z)
{
    if(x>=y)
    {
        if(z>=x){printf("小林 > 小明 > 小强");}
        else if(z>=y){printf("小明 > 小林 > 小强");}
        else{printf("小明 > 小强 >小林");}
    }
    else if(z>=y)
    {
       printf("小林 > 小强 > 小明");
    }
    else
    {
        if(z>=x){printf("小强 > 小林 > 小明");}
        else{printf("小强 > 小明 > 小林");}
    }
    
}

int main(void)
{
    printf("请按小明，小强，小林的顺序输入三人的成绩\n");
    int x1, x2, x3;  
    int y1, y2, y3;  
    int z1, z2, z3;
   printf("请输入小明的三项成绩(顺序为A B C,以一个空格为间隔）：");
    scanf("%d %d %d",&x1,&x2,&x3);
    int p1=average(x1,x2,x3);
    int f1=fangcha(x1,x2,x3);
    int zh1 = score(p1,f1);
   printf("请输入小强的三项成绩(顺序为A B C,以一个空格为间隔）：");
    scanf("%d %d %d",&y1,&y2,&y3);
    int p2=average(y1,y2,y3);
    int f2=fangcha(y1,y2,y3);
    int zh2 = score(p2,f2);
   printf("请输入小林的三项成绩(顺序为A B C,以一个空格为间隔）：");
    scanf("%d %d %d",&z1,&z2,&z3);
    int p3=average(z1,z2,z3);
    int f3=fangcha(z1,z2,z3);
    int zh3 = score(p3,f3);   
    paiming(zh1,zh2,zh3);
    return 0;

}
```
### 第三部分
1. 结果:`main`里面的变量`a`仍然是 10，`b`仍然是 20，没有完成交换。原因：1. `main`调用`swap(a,b)`的时候，会把`main`中`a=10`、`b=20`这两个值(实参)拷贝一份，传给 swap 函数的形参`a`和`b`。而swap 里面的`a`、`b`是swap 函数自己的局部变量，和 main 里的 a、b 是两块完全独立的内存，只是名字碰巧一样。swap 内部只交换了这份拷贝副本，函数执行结束后 swap 的局部变量直接销毁。 main 函数里原始变量完全不受影响。
2. 修改后的代码如下：
```c
void swap(int *a, int *b){ 
  int temp = *a;  
  *a = *b;        
  *b = temp;       
}
int main(){
  int a = 10;
  int b = 20;
  swap(&a, &b);   
}

